name: Deploy Node.js project to Azure Function App

on:
  push:
    branches: ["main"]

env:
  AZURE_FUNCTIONAPP_NAME: 'vuego-demoapp'
  NODE_VERSION: '20.x'

jobs:
  build-and-deploy:
    runs-on: windows-latest
    environment: dev

    steps:
      # 1. ดึงโค้ดจาก GitHub
      - uses: actions/checkout@v3

      # 2. ตั้ง Node.js version
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: ${{ env.NODE_VERSION }}

      # 3. ติดตั้ง dependencies (ไปที่ folder client)
      - name: Install dependencies
        run: npm install

      # 4. Build โปรเจกต์
      - name: Build
        run: npm run build
